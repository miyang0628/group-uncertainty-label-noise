# GUARD — local GPU environment requirements
# Python 3.10–3.11 recommended.
#
# IMPORTANT: install PyTorch with the CUDA build that matches your GPU driver.
# Do NOT rely on the plain "torch" below if you need a specific CUDA version.
# See https://pytorch.org/get-started/locally/  e.g. for CUDA 12.1:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#
# Then:  pip install -r requirements.txt

# --- scientific stack ---
numpy>=1.26,<3
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
pyarrow>=15.0            # parquet I/O for predictions/metadata

# --- plotting (grayscale figures, dpi 600) ---
matplotlib>=3.7
seaborn>=0.13

# --- deep learning / HerBERT fine-tuning ---
torch>=2.1              # install the CUDA wheel separately (see note above)
transformers>=4.40
datasets>=2.19
accelerate>=0.30
tokenizers>=0.19
huggingface-hub>=0.23
sentencepiece>=0.2      # required by HerBERT / XLM-R tokenizers

# --- label-noise baseline (RQ1) ---
cleanlab>=2.6           # confident learning

# --- utilities & notebooks ---
tqdm>=4.66
requests>=2.31
jupyter>=1.0
ipykernel>=6.29
nbformat>=5.10
